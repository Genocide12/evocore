<div align="center">

# 🧬 Эво

### Существо, которое пишет свой код прямо у тебя на глазах

**Нажми одну кнопку — и смотри.** Эво пробует версии своего кода, замеряет их реальную
скорость, выбрасывает неудачные и **перезаписывает собственный файл** — растёт из
яичка 🥚 в легенду 🐉 у тебя на экране.

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)
[![Python](https://img.shields.io/badge/python-3.9%2B-blue.svg)](https://www.python.org/)
[![Node](https://img.shields.io/badge/node-20%2B-green.svg)](https://nodejs.org/)
[![Next.js](https://img.shields.io/badge/Next.js-16-black.svg)](https://nextjs.org/)

![Эво в действии](docs/dashboard.png)

</div>

---

## 🎮 Как играть

Это не игра — всё по-настоящему. Но правила простые:

1. Открой сайт и нажми **«🚀 Расти!»**
2. Эво создаёт мутантов своего кода и **замеряет их реальную скорость** на эталонной задаче
3. Мутант оказался быстрее и не сломался? Эво **переписывает свой собственный код** — ты видишь новые строки прямо на экране
4. Следи, как растёт **«Сила ×N»**, как Эво получает уровни, а за каждый рекорд — праздничное конфетти 🎊

Слушать нечего и читать почти не надо — всё видно. Работает и с телефона.

## 🥚 Уровни Эво

| Уровень | Сила | | Уровень | Сила |
|---|---|---|---|---|
| 🥚 Яичко | ×1.0 | | 🦉 Мастер | ×2.5 |
| 🐣 Птенчик | ×1.1 | | 🧠 Гений | ×3.5 |
| 🐥 Умник | ×1.35 | | 🧙 Волшебник | ×5.0 |
| 🦜 Знаток | ×1.8 | | 🐉 Легенда | ×7.0 |

Сила = во сколько раз Эво быстрее своей наивной версии. Обычно первый скачок
(🥚 → 🧠, примерно ×4–5) происходит в первые секунды — дальше интереснее.

## 🤔 Это фокус?

Нет. Загляни в папку `evolution/` после запуска:

- **`genome_core.py`** — это тот самый код, что показан на сайте. Он **меняется на диске** после каждого улучшения. Открой файл в редакторе и сравни: было → стало.
- Внутри — настоящий **генетический алгоритм** (~700 строк Python, только стандартная библиотека): популяция, мутации, кроссовер, отбор.
- Каждое улучшение проходит **два барьера**: тест на правильность (совпадение с эталоном до 1e-9) и дуэль «старый код против нового» в одном окне замеров — без обмана.
- Если Эво застревает, он зовёт подмогу: включается **иммиграция** свежих мутантов.

## ⚡ Быстрый старт

```bash
git clone https://github.com/Genocide12/evocore.git
cd evocore
npm install
npm run build
npm start          # открой http://localhost:3000
```

Нужен Python 3.9+ рядом с Node — движок запускается как процесс `python3`.

**Docker** (всё уже внутри):

```bash
docker build -t evocore .
docker run -p 3000:3000 evocore
```

**Railway / Render / Fly.io** — просто подключи репозиторий, Dockerfile подхватится
автоматически. В Railway: Settings → Networking → Generate Domain, порт `3000`.

## 💻 Консольный режим

Дашборд не обязателен — Эво умеет расти прямо в терминале:

```bash
python3 evolution/evolution_core.py --generations 100 --population 16
python3 evolution/evolution_core.py --reset        # вернуть Эво в яичко
```

## 🧠 Как это устроено

```
   мутации + кроссовер
          ↓
   популяция кандидатов
          ↓
   бенчмарк: честная скорость каждого
          ↓
   гейт правильности + дуэль с чемпионом
          ↓   (победил и быстрее ≥1.5%)
   ПЕРЕЗАПИСЬ ГЕНОМА genome_core.py
          ↓
   организм исполняет свой новый код (метаболизм)
          ↓
   стагнация? → всплеск иммиграции
```

Дашборд (Next.js) лишь показывает то, что делает движок: опрашивает его состояние
раз в секунду и рисует существо, код и дневник.

```
evocore/
├── evolution/
│   ├── evolution_core.py   # движок: генетический алгоритм
│   └── genome_core.py      # живой геном — файл, который Эво перезаписывает
├── src/
│   ├── app/                # дашборд + API (/api/evolution/*)
│   ├── components/pet/     # существо, окно кода, конфетти
│   └── lib/                # управление процессом движка, уровни
├── Dockerfile
└── package.json
```

## 🛠 API (если хочешь поиграться)

| Метод | Путь | Что делает |
|---|---|---|
| GET | `/api/evolution/status` | состояние Эво: поколение, сила, код, события |
| POST | `/api/evolution/control` | `{"action": "start\|stop\|reset", "generations": 200, "population": 16}` |

## Лицензия

MIT — делай что хочешь 🎉
